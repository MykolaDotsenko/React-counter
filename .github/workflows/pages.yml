name: Publish Pulse Counter and guarded QA

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: gh-pages-publish
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build public Pulse Counter
        run: npm run build -- --base=/React-counter/

      - name: Build guarded shopping timing QA
        env:
          VITE_SHOPPING_SHELL: "1"
          VITE_SHOPPING_QA_TIMING: "1"
        run: |
          npm run build -- --base=/React-counter/qa/ --outDir=dist-qa
          npm run qa:prepare-index

      - name: Stage static site
        run: |
          rm -rf /tmp/pulse-counter-pages
          mkdir -p /tmp/pulse-counter-pages
          cp -R dist/. /tmp/pulse-counter-pages/
          mkdir -p /tmp/pulse-counter-pages/qa
          cp -R dist-qa/. /tmp/pulse-counter-pages/qa/
          touch /tmp/pulse-counter-pages/.nojekyll

      - name: Publish gh-pages branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git switch --orphan gh-pages
          git rm -rf . || true
          cp -R /tmp/pulse-counter-pages/. .
          git add -A
          git commit -m "deploy: Pulse Counter + guarded QA ${GITHUB_SHA}"
          git push --force origin gh-pages
