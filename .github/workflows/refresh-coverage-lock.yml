name: Refresh coverage lockfile

on:
  push:
    branches: [chore/risk-based-coverage]

permissions:
  contents: write

concurrency:
  group: refresh-coverage-lock-${{ github.ref }}
  cancel-in-progress: true

jobs:
  refresh-lockfile:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: chore/risk-based-coverage

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .node-version
          cache: npm

      - name: Regenerate lockfile with repository npm version
        run: npx --yes npm@11.19.0 install --package-lock-only --ignore-scripts

      - name: Commit lockfile only when changed
        shell: bash
        run: |
          if git diff --quiet -- package-lock.json; then
            echo "package-lock.json already synchronized"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "chore: synchronize coverage lockfile"
          git push origin HEAD:chore/risk-based-coverage
